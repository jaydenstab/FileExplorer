import type {
  ExplorerFeedback,
  ExplorerFilters,
  ExplorerPreview,
  ExplorerReindex,
  ExplorerSearch,
} from './useExplorerController';

export function buildExplorerFilters(input: ExplorerFilters): ExplorerFilters {
  return input;
}

export function buildExplorerSearch(input: ExplorerSearch): ExplorerSearch {
  return input;
}

export function buildExplorerReindex(input: ExplorerReindex): ExplorerReindex {
  return input;
}

export function buildExplorerPreview(input: ExplorerPreview): ExplorerPreview {
  return input;
}

export function buildExplorerFeedback(input: ExplorerFeedback): ExplorerFeedback {
  return input;
}
