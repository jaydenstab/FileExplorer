Django>=5.2,<6.0
PyMuPDF>=1.23.0
# ML stack: exact pins for Python 3.13 + avoid meta-tensor (reranker use_fp16=False, devices="cpu")
transformers==4.47.0
torch==2.6.0
accelerate==0.32.0
sentence-transformers==2.7.0
# FlagEmbedding pulls peft; unbounded peft (e.g. 0.18+) breaks transformers==4.47.0 at import time.
peft==0.13.2
FlagEmbedding==1.3.5
chromadb>=0.4.0
Pillow>=10.0.0
django-cors-headers>=4.0.0
python-dotenv>=1.0.0
